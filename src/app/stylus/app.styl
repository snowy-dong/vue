.tab {
	display: flex;
	width: 100%;
	height: 40px;
	line-height: 40px;
	border-1px(rgba(7, 17, 27 0.1));

	.tab-item {
		flex: 1;
		text-align: center;

		&>a {
			display: block;
			text-decoration: none;

			&.router-link-active {
				color: #c40000;
			}
		}
	}
}
